export enum UserRole {
    ADMIN = "admin",
    EDITOR = "editor",
    DEFAULT = "default",
}

export enum UserSex {
    MALE = "male",
    FEMALE = "female",
    OTHER = "other",
    NOT_SPECIFIED = "not specified",
}

export enum AgreementStatus {
    AGREE="agree",
    DISAGREE="disagree",
    UNSURE="unsure",
    NOT_SPECIFIED="not specified",
}