export enum UserRole {
    ADMIN = "admin",
    EDITOR = "editor",
    DEFAULT = "default",
}

export enum UserSex {
    MALE = "male",
    FEMALE = "female",
    OTHER = "other",
    NOT_SPECIFIED = "not specified",
}